const textPara = `While we strive for accurate configurator visualizations, there are
  some options that may not be rendering accurately at this time. We are
  working on this, but in the meantime, we recommend consulting with
  your Porsche Center for the final details. Thank you!`;


  /*<div className="garage" onClick={() => setOpenDialog(!openDialog)}>
          Garage: 0
        </div>
        <div className="garage" onClick={() => setOpenDialog(!openDialog)}>
          Calculate payment
        </div>
        <div className="price-Area">
          <div>
            <img src={CarSvg} alt="carSvg" />
          </div>
          <div>
            <p>
              find a Dealer <br />
              <span>$160,000</span>
            </p>
          </div>
        </div>
        
        
        
        
        <PorscheCards
          selected={selected}
          handleOpenBuild={handleOpenBuild}
          openBuild={openBuild}
          handleChangeCar={handleChangeCar}
        />
        {openBuild ? (
          <>
            <BuildPorsche PorscheCar={PorscheCar} />
          </>
        ) : (
          <>
           <PCardForm/>
           <PorscheCards/>
          </>
        )}
        */
  export default textPara